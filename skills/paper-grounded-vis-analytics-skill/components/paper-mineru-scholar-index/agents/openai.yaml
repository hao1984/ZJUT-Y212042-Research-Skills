interface:
  display_name: "Paper MinerU Scholar Index"
  short_description: "Build MinerU paper metadata indexes"
  default_prompt: "Use $paper-mineru-scholar-index to ingest these PDFs into paper.md/images/meta.json and rebuild the paper retrieval index."

policy:
  allow_implicit_invocation: true
